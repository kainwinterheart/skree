-
    name: Example group
    module:
        path: /var/lib/skree/modules/perl.so
        config:
            lib:
                - /var/www/muh_kool_project
            package: MuhKooProject::Skree
    events:
        - id: ExampleEvent
